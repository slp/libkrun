[workspace]
members = ["src/libkrun"]

[profile.dev]
#panic = "abort"

[profile.release]
#panic = "abort"
lto = true

[patch.crates-io]
kvm-ioctls = { path = "../kvm-ioctls" }
kvm-bindings = { path = "../kvm-bindings" }