[workspace]
members = ["src/libkrun"]

[profile.dev]
#panic = "abort"

[profile.release]
#panic = "abort"
lto = true

[patch.crates-io]
#kvm-bindings = { path = '/root/src/kvm-bindings' }
kvm-ioctls = { path = '/root/src/kvm-ioctls' }